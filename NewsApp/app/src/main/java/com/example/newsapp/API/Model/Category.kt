package com.example.newsapp.API.Model

import androidx.compose.ui.graphics.Color

data class Category(val categoryBackground: Color,
                    val categoryName: String,
                    val categoryImage:Int)
